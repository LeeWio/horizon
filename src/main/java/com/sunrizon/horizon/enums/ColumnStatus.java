package com.sunrizon.horizon.enums;

public enum ColumnStatus {
    DRAFT,      // Column is in draft status, not visible to users
    PUBLISHED,  // Column is published and visible
    ARCHIVED    // Column is archived, not visible but kept for records
}