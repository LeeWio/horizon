package com.sunrizon.horizon.enums;

public enum SeriesStatus {
    DRAFT,      // Series is in draft status, not visible to users
    PUBLISHED,  // Series is published and visible
    ARCHIVED    // Series is archived, not visible but kept for records
}