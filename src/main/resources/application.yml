file:
  upload-dir: uploads

spring:
  mail:
    host: smtp.qq.com
    username: 3499508634@qq.com
    password: hddhucgnnljwchbh
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          ssl:
            enable: true

  jackson:
    time-zone: GMT+8
    date-format: yyyy-MM-dd HH:mm:ss

  datasource:
    url: jdbc:mysql://localhost:3306/blog?useSSL=false&serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf8
    username: root
    password: "Steve Jobs"
    driver-class-name: com.mysql.cj.jdbc.Driver

  redis:
    host: 127.0.0.1
    port: 6379
    database: 0

  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest

  jpa:
    hibernate:
      ddl-auto: update      # 自动建表或更新表结构
    show-sql: true
    open-in-view: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080

# 可选日志配置
# logging:
#   level:
#     root: INFO
#     org.springframework: DEBUG

# 可选 OpenAPI / Swagger 配置
# springdoc:
#   api-docs:
#     path: /v3/api-docs
#   swagger-ui:
#     path: /swagger-ui.html
#     enabled: true
#     operationsSorter: method
#   show-actuat: true
